class CorrectiveAction < ActiveRecord::Base
  # belongs_to :incident
  # validates :name, :responsible, presence: true
  
  # # use audited for model record history
  # audited
  # # use papertrail for model record history
  # has_paper_trail

end
