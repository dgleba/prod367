
<row class="row is-table-row" >

  <div class="col-sm-1 ">
    <%= link_to 'Create', new_dc_level_path , class: "btn btn-success"%>
  </div>

  <div class="col-sm-2 ">
    <h4>Listing Dc Levels</h4>
  </div>

  <div class="col-sm-3">
    <%= search_form_for @q, class: 'search-form' do |f| %>
      <div class="input-group">
        <%= f.search_field :name_cont, class: 'form-control' %>
        <span class="input-group-btn">
          <button class="btn btn-primary">Search <i class="fa fa-search"></i>
          </button>
        </span>
      </div>
    <% end %>
  </div>

  <div class="col-sm-4 ">
    <%= paginate @dc_levels %>
  </div>
  
  <div class="col-sm-2 ">
    <%= page_entries_info @dc_levels, :entry_name => 'item' %>
  </div>
  
</row>


<table class="table table-striped">
  <thead>
    <tr>
      <th colspan="1"></th>
            <th>Name</th>
      <th>Active status</th>
      <th>Sort order</th>
      <th>Comment</th>
    </tr>
  </thead>

  <tbody>
    <% @dc_levels.each do |dc_level| %>
      <tr>
        <td>
          <div class="btn-group">
          <%= link_to 'Show', dc_level, class: "btn btn-info", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Edit', edit_dc_level_path(dc_level), class: "btn btn-primary", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Destroy', dc_level, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-warning", :style=>'color:#FFFFFF;' %>
          </div>
        </td>
        
        <td><%= dc_level.name %></td>
        <td><%= dc_level.active_status %></td>
        <td><%= dc_level.sort_order %></td>
        <td><%= dc_level.comment %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
