
<row class="row is-table-row" >

  <div class="col-sm-1 ">
    <%= link_to 'Create', new_employee_path , class: "btn btn-success"%>
  </div>

  <div class="col-sm-2 ">
    <h4>Listing Employees</h4>
  </div>

  <div class="col-sm-3">
    <%= search_form_for @q, class: 'search-form' do |f| %>
      <div class="input-group">
        <%= f.search_field :name_cont, class: 'form-control' %>
        <span class="input-group-btn">
          <button class="btn btn-primary">Search <i class="fa fa-search"></i>
          </button>
        </span>
      </div>
    <% end %>
  </div>

  <div class="col-sm-4 ">
    <%= paginate @employees %>
  </div>
  
  <div class="col-sm-2 ">
    <%= page_entries_info @employees, :entry_name => 'item' %>
  </div>
  
</row>


<table class="table table-striped">
  <thead>
    <tr>
      <th colspan="1"></th>
            <th>Name</th>
      <th>Clock</th>
      <th>Active status</th>
      <th>Sort</th>
    </tr>
  </thead>

  <tbody>
    <% @employees.each do |employee| %>
      <tr>
        <td>
          <div class="btn-group">
          <%= link_to 'Show', employee, class: "btn btn-info", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Edit', edit_employee_path(employee), class: "btn btn-primary", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-warning", :style=>'color:#FFFFFF;' %>
          </div>
        </td>
        
        <td><%= employee.name %></td>
        <td><%= employee.clock %></td>
        <td><%= employee.active %></td>
        <td><%= employee.sort %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
